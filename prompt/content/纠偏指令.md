你刚刚生成的代码违反了六边形架构，请修正：

问题：
- 扇出逻辑放在 Domain 层（应在 Application 或 Adapter 层）
- 媒体 URL 硬编码（应走配置中心）
- 时间线查询没走 QueryPort（直接返回了 Domain 对象）
- medias JSON 转换逻辑混入 Domain 层

请重新生成，并严格遵守以下规则：
- Domain 纯净，只包含业务规则校验
- Repository 操作聚合，JSON 转换在 Adapter 层处理
- Controller 只能调用 UseCase
- 扇出逻辑在 ApplicationService 编排，具体写入由 Adapter 层实现
- 时间线查询必须走 QueryPort，返回 DTO 而非 Domain
