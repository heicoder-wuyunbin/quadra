你刚刚生成的代码违反了六边形架构，请修正：

问题：
- 计数更新放在本服务（应通过事件跨域）
- LIKE/COMMENT 拆成两张表（应统一模型）
- 直接 SQL 更新 content 表计数（应走 Outbox 事件）

请重新生成，并严格遵守以下规则：
- Domain 纯净
- Repository 操作聚合
- Controller 只能调用 UseCase
- Interaction 统一表模型（action_type 区分 LIKE/COMMENT）
- 计数更新必须通过 InteractionCreatedEvent / InteractionCanceledEvent 走 Outbox 通知 content 域
