你刚刚生成的代码违反了六边形架构，请修正：

问题：
- Controller 直接调用了 Mapper
- Domain 出现了 Spring 注解
- Repository 变成 DAO
- 定时任务逻辑放在了 Domain 层（应在 Adapter-out/scheduler）
- 评分规则硬编码在 Domain（应外置可替换）
- 直接跨库查询用户/内容数据（应走事件或 RPC Port）

请重新生成，并严格遵守以下规则：
- Domain 纯净，无框架依赖
- Repository 操作聚合
- Controller 只能调用 UseCase
- 定时任务调度器放在 Adapter-out 层
- 评分规则通过策略接口外置，便于测试和替换
- 跨域数据通过 Port 接口或领域事件获取
