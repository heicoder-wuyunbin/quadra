你刚刚生成的代码违反了六边形架构，请修正：

问题：
- Controller 直接操作 Redis（应通过 Port）
- Domain 依赖 Redis（应纯 Java）
- 关注关系查询没走 QueryPort
- Repository 变成 DAO

请重新生成，并严格遵守以下规则：
- Domain 纯净（无框架依赖）
- Repository 操作聚合
- Controller 只能调用 UseCase
- 读操作必须走 QueryPort
- Redis 操作封装在 adapter-out 层
