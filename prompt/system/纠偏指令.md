你刚刚生成的代码违反了六边形架构，请修正：

问题：
- Controller 直接调用了 Mapper
- Domain 出现了 Spring 注解
- Repository 变成 DAO
- 权限校验逻辑放在 Controller（应在拦截器/AOP）
- 管理端与用户端共用 JWT 认证（应隔离 Scope）
- 审计日志写入没有与业务事务一致
- RBAC 权限加载没走缓存，导致 N+1 查询

请重新生成，并严格遵守以下规则：
- Domain 纯净，无框架注解
- Repository 操作聚合
- Controller 只能调用 UseCase
- 权限校验逻辑必须在拦截器或 AOP 中实现
- 管理端 JWT 必须使用独立 Scope，与用户端隔离
- 审计日志必须与业务操作在同一事务内
- RBAC 权限读取必须走缓存优化
