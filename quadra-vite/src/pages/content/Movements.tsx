import { Typography } from 'antd';

const { Title } = Typography;

const Movements: React.FC = () => {
  return (
    <div>
      <Title level={2}>动态审核</Title>
      <p>图文动态审核页面（待开发）</p>
    </div>
  );
};

export default Movements;
