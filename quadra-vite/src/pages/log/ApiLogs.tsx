import { Typography } from 'antd';

const { Title } = Typography;

const ApiLogs: React.FC = () => {
  return (
    <div>
      <Title level={2}>接口日志</Title>
      <p>接口日志页面（待开发）</p>
    </div>
  );
};

export default ApiLogs;
