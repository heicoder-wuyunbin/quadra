import { Typography } from 'antd';

const { Title } = Typography;

const LoginLogs: React.FC = () => {
  return (
    <div>
      <Title level={2}>登录日志</Title>
      <p>登录日志页面（待开发）</p>
    </div>
  );
};

export default LoginLogs;
