import { Typography } from 'antd';

const { Title } = Typography;

const MessageRecords: React.FC = () => {
  return (
    <div>
      <Title level={2}>推送记录</Title>
      <p>推送记录页面（待开发）</p>
    </div>
  );
};

export default MessageRecords;
