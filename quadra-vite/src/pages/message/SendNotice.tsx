import { Typography } from 'antd';

const { Title } = Typography;

const SendNotice: React.FC = () => {
  return (
    <div>
      <Title level={2}>发送站内信</Title>
      <p>发送站内信页面（待开发）</p>
    </div>
  );
};

export default SendNotice;
