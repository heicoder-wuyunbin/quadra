import { Typography } from 'antd';

const { Title } = Typography;

const Templates: React.FC = () => {
  return (
    <div>
      <Title level={2}>消息模板</Title>
      <p>消息模板管理页面（待开发）</p>
    </div>
  );
};

export default Templates;
