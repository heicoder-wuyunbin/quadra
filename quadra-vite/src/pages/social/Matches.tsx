import { Typography } from 'antd';

const { Title } = Typography;

const Matches: React.FC = () => {
  return (
    <div>
      <Title level={2}>匹配记录</Title>
      <p>匹配记录页面（待开发）</p>
    </div>
  );
};

export default Matches;
