import { Typography } from 'antd';

const { Title } = Typography;

const Audit: React.FC = () => {
  return (
    <div>
      <Title level={2}>用户审核</Title>
      <p>用户审核页面（待开发）</p>
    </div>
  );
};

export default Audit;
