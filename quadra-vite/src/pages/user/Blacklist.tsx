import { Typography } from 'antd';

const { Title } = Typography;

const Blacklist: React.FC = () => {
  return (
    <div>
      <Title level={2}>黑名单管理</Title>
      <p>黑名单管理页面（待开发）</p>
    </div>
  );
};

export default Blacklist;
